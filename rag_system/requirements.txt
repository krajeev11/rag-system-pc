# RAG System Requirements - Using LangChain and Chroma
# 
# ⚠️ WARNING: Installing all at once can freeze your laptop!
# See SAFE_INSTALLATION_GUIDE.md for step-by-step installation
#
# Recommended: Install in steps:
#   1. pip install langchain langchain-community chromadb numpy --no-cache-dir
#   2. pip install sentence-transformers --no-cache-dir  (this is heavy!)
#   3. pip install gradio --no-cache-dir  (optional, for web UI)

# Core dependencies (install these first - lightweight)
langchain>=0.1.0
langchain-community>=0.0.20
chromadb>=0.4.0
numpy>=1.24.0

# Heavy dependency - install separately to avoid freezing!
# Run: pip install sentence-transformers --no-cache-dir
sentence-transformers>=2.2.0

# Web UI dependencies (optional - install separately)
gradio>=4.0.0
# streamlit>=1.28.0  # Uncomment if you prefer Streamlit over Gradio

# Optional: For OpenAI integration (no heavy downloads needed)
# openai>=1.0.0
# langchain-openai>=0.0.5

# Optional: For Anthropic integration
# anthropic>=0.18.0
# langchain-anthropic>=0.1.0

# Optional: For local LLM models (very heavy!)
# transformers>=4.30.0
# torch>=2.0.0
